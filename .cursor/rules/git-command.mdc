---
description: Cuando el usuario escribe "git" significa add y commit (sin push)
alwaysApply: true
---

# Comando "git" del usuario

Cuando el usuario escriba solo **"git"** (o pida "git", "haz git", etc.), interpretar como:

1. `git add .`
2. `git commit` (usar un mensaje descriptivo según los cambios recientes, o preguntar si hace falta)

**No** incluir `git push` salvo que lo pida explícitamente.

Ejecutar con permiso `git_write` para add y commit.
