# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Desktop.ini

# Editor / IDE
.idea
.vscode
*.swp
*.swo
*~

# Entorno y secretos
.env
.env.*
!.env.example

# Dependencias (por si se instalan en raíz)
node_modules

# Logs y temporales
*.log
.tmp
.cache

# Builds y artefactos (por si se generan en raíz)
dist
build
.next
out
coverage

############################
# NEXT.JS (subcarpeta)
############################
senderos-de-chiapas/node_modules
senderos-de-chiapas/.next
senderos-de-chiapas/out
senderos-de-chiapas/build
senderos-de-chiapas/coverage
senderos-de-chiapas/*.tsbuildinfo

############################
# STRAPI (backend)
############################
backend/node_modules
backend/dist
backend/build
backend/.cache
backend/.tmp